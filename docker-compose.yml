name: spydf

services:
  spydf:
    build: .
    environment:
      HOST: 0.0.0.0
      PORT: 8765
      # every other setting has a default; see .env.example for the full list
      # SPYDF_LOG_LEVEL: INFO
      # SPYDF_LOG_FILE: /var/log/spydf.log
      # SPYDF_LOG_FILENAMES: "1"   # opt-in: log uploaded file names
    ports:
      - "8765:8765"
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
